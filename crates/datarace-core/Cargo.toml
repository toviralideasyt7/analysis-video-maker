[package]
name = "datarace-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic high-performance core for analysis-video-maker: ingest, normalize, rank, frame-tape, validate, hash, probe."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
csv = "1.3"
calamine = "0.26"
regex = "1.10"
anyhow = "1.0"
sha2 = "0.10"
chrono = { version = "0.4", features = ["serde"] }
rayon = "1.10"